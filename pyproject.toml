[project]
name = "cosmos"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "imageio>=2.31",
    "imageio-ffmpeg",
    "lerobot>=0.4",
    "numpy>=1.24",
    "pyarrow",
]
