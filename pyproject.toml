[project]
name = "cosmos"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "imageio>=2.31",
    "imageio-ffmpeg",
    "lerobot>=0.4",
    "numpy>=1.24",
    "pyarrow",
    "torch>=2.7.1",
    "transformers>=4.57.6",
    "unsloth>=2026.2.1",
]
